export default class FirePlace {
    constructor(position, sprite, spriteAnimation) {
        this.position = position
        this.sprite = sprite
        this.spriteAnimation = spriteAnimation    
    }
}